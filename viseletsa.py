import os

def gen_ind(st, fel):
    res = []
    el = 0
    while st:
        if st[el]

fword = input('Введите слово')
os.system('CLS')
mist = 0
resword = '_' * len(fword)
while mist != 5:
    print(resword)
    let = input('Какую букву откроем?')
    if let in fword:
        if fword.count(let) > 1:



